function bounds = rect_padded_face_large(calibration_data, key_map, padding_info, const, screen_rect, dist)

bounds = bfw.calibration.rect_padded_face_impl( calibration_data, key_map, padding_info, const, screen_rect, dist );

end